%section{class: "w-full sm:w-1/2 lg:w-1/3 mx-auto mb-4 mt-4"}
  - flash.each do |type, message|
    - color = type == "notice" ? "blue" : "red"
    .p-2.bg-white.rounded.shadow-lg.border-2{class: "border-#{color}-700"}
      %div{class: "text-#{color}-500"}= message
